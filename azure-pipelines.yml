# ASP.NET Core (.NET Framework)
# Build and test ASP.NET Core projects targeting the full .NET Framework.
# Add steps that publish symbols, save build artifacts, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/dotnet-core

trigger:
- main

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'
    msbuildArgs: '/p:DeployOnBuild=true /p:WebPublishMethod=Package /p:PackageAsSingleFile=true /p:SkipInvalidConfigurations=true /p:DesktopBuildPackageLocation="$(build.artifactStagingDirectory)\WebApp.zip" /p:DeployIisAppPath="Default Web Site"'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  inputs:
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
- task: DockerCompose@1
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: 'Pay-As-You-Go(dcf7491b-e7b9-40c2-bbd1-51ac080d654f)'
    azureContainerRegistry: '{"loginServer":"mcstest-b3ftbyb9hwevgfbr.azurecr.io", "id" : "/subscriptions/dcf7491b-e7b9-40c2-bbd1-51ac080d654f/resourceGroups/rg-mitul/providers/Microsoft.ContainerRegistry/registries/MCSTest"}'
    dockerComposeFile: '**/docker-compose.yml'
    action: 'Build services'
- task: DockerCompose@1
  inputs:
    containerregistrytype: 'Azure Container Registry'
    azureSubscription: 'Pay-As-You-Go(dcf7491b-e7b9-40c2-bbd1-51ac080d654f)'
    azureContainerRegistry: '{"loginServer":"mcstest-b3ftbyb9hwevgfbr.azurecr.io", "id" : "/subscriptions/dcf7491b-e7b9-40c2-bbd1-51ac080d654f/resourceGroups/rg-mitul/providers/Microsoft.ContainerRegistry/registries/MCSTest"}'
    dockerComposeFile: '**/docker-compose.yml'
    action: 'Push services'
    includeLatestTag: true